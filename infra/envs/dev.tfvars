environment  = "dev"
azure_region = "brazilsouth"
openai_model = "gpt-5.4-mini"

# The teammate's subscription already owns the unsuffixed global names
# (kv-hotelia-dev, sthoteliadev001, ...); this keeps ours unique.
global_suffix = "sc"
